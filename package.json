{
  "name": "consent-aware-voice-lead-pilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A dependency-free, testable Vapi webhook pilot for consent-based lead qualification.",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
